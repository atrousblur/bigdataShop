<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.kitri.bigdataShop.board">

	<insert id="insert" parameterType="board">
		insert into board values
		(board_seq.nextval, #{id}, #{title}, #{text}, 0, '1',
		to_char(sysdate), to_char(sysdate))
	</insert>

	<select id="listall" resultType="board">
		select * from board order by
		board_no desc
	</select>

	<insert id="insertfile">
		insert all
		into board_file(board_no,file_name) values(board_seq.currval, #{file_name})
		into board_file(board_no,file_name) values(board_seq.currval, #{file_name})
		into board_file(board_no,file_name) values(board_seq.currval, #{file_name})
		into board_file(board_no,file_name) values(board_seq.currval, #{file_name})
		into board_file(board_no,file_name) values(board_seq.currval, #{file_name})
		select * from dual;
	</insert>
</mapper>